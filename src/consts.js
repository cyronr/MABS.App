//ERRORS
export const ERROR_DEFAULT_MSG = 'Nieoczekiwany błąd aplikacji :(';

export const ProfileType = Object.freeze({
    Facility: 1,
    Patient: 2,
    Admin: 4
});

export const AppointmentStatus = Object.freeze({
    Prepared: 1,
    Confirmed: 2,
    Cancelled: 3
});